# 複製成 keystore.properties（同層目錄）再填入實際值。
# keystore.properties 已在 .gitignore，不會進版控——密碼絕不進 git。
#
# 產金鑰（一次性，密碼自己設）：
#
#   keytool -genkeypair -v -keystore <你的路徑>/treadless-release.jks \
#     -alias treadless -keyalg RSA -keysize 4096 -validity 10000
#
# Java 17 的 keytool 產 PKCS12 格式，金鑰密碼與金鑰庫密碼是同一組，
# 下面兩個 password 欄位填一樣的值即可。
#
# 【雷】側載分發沒有 Play App Signing 當備援：這把 .jks 掉了或密碼忘了，
# 就再也發不出「使用者能直接覆蓋安裝」的更新，只能請他們解除安裝重裝
# （＝資料全消）。備份到密碼管理器或離線碟。
#
# 沒有這個檔也建得起來，只是 assembleRelease 會產出 unsigned APK。

storeFile=/path/to/treadless-release.jks
storePassword=
keyAlias=treadless
keyPassword=
